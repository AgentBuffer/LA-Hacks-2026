[project]
name = "agentbuffer-shared"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "supabase>=2.0",
]

[tool.setuptools.packages.find]
exclude = ["tests", "tests.*"]
