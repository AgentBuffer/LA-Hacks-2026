[project]
name = "agentbuffer-publisher"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "uagents>=0.14",
    "agentbuffer-shared",
    "httpx>=0.27",
]

[tool.uv.sources]
agentbuffer-shared = { workspace = true }
