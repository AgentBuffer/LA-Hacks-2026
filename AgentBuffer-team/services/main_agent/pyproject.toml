[project]
name = "agentbuffer-main-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "uagents>=0.20.0",
    "openai",
    "agentbuffer-shared",
]

[tool.uv.sources]
agentbuffer-shared = { workspace = true }

[tool.setuptools.packages.find]
exclude = ["tests", "tests.*"]
